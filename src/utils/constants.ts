export const MAX_STR_LEN = 255;
export const MAX_STR_LEN_EXT = 2000;
export const MIN_PASSWORD_LEN = 8;
export const MAX_PASSWORD_LEN = 24;

// Auth
export const VERIFICATION_TOKEN_EXPIRES_IN_HOURS = 1;
export const PASSWORD_RESET_TOKEN_EXPIRES_IN_HOURS = 0.25; //
export const VERIFICATION_TOKEN_LEN = 128;
export const ACCESS_TOKEN_EXPIRES_IN_MS = 15 * 60 * 1000; // 15 minutes
export const SESSION_EXPIRES_IN_MS = 7 * 24 * 60 * 60 * 1000; // 7 days
export const REFRESH_TOKEN_LEN = 128;

// Uploads
export const MAX_FILE_SIZE = 100 * 1024 * 1024; // 100MB
export const MAX_FILES_PER_UPLOAD = 10;

// Deletes
export const MAX_FILES_PER_DELETE = 20;

// Query
export const QUERY_LIMIT_SM = 10;
export const QUERY_LIMIT = 20;
export const QUERY_LIMIT_LG = 50;

// Locations
export const LOCATION_SEARCH_RADIUS_IN_KM = 20;

// Evidence
export const MAX_EVIDENCE_FILES = 10;
